# Walkthrough - Project Size Optimization

I have successfully optimized the project to be well under the 100MB limit for your submission.

## Changes Made

### 1. File Cleanup
- **Deleted `24125015.zip`**: This single file was 142MB and was the primary cause of the size issue.
- **Deleted `24125015/` folder**: Removed the redundant copy of the project.
- **Deleted unused assets**: Removed `screenshot_wg68fneb.png`.

### 2. Build Optimization
- **Enabled R8 Minification**: Set `isMinifyEnabled = true` in `app/build.gradle.kts`. This will shrink the code in your release APK.
- **Enabled Resource Shrinking**: Set `isShrinkResources = true` in `app/build.gradle.kts`. This removes unused resources from the APK.

### 3. Cache Cleanup
- **Performed `./gradlew clean`**: This removed all generated build files (the `build/` folders), which are usually large but should not be included in a source code submission.

## Recommendations for Submission
When zipping the project for nộp bài, make sure to **exclude** these folders (they are generated by Android Studio/Gradle and will be recreated by the recipient):
- `.gradle/`
- `.idea/`
- `build/` (already cleaned)
- `.artifacts/` (if your instructor doesn't need these logs)

> [!TIP]
> After these deletions, your source code directory should be very small (likely under 10MB), leaving plenty of room for the 100MB limit.
